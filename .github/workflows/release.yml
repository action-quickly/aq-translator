name: Release Plugin

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read plugin info
        id: info
        run: |
          VERSION=$(jq -r '.version' plugin.json)
          ID=$(jq -r '.id' plugin.json)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "id=$ID" >> $GITHUB_OUTPUT
          echo "Plugin: $ID v$VERSION"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci || true

      - name: Build plugin
        run: |
          if [ -f "package.json" ]; then
            npm run build || true
          fi

      - name: Create plugin zip
        run: |
          mkdir -p dist
          zip -r dist/aq-${{ steps.info.outputs.id }}.zip . \
            -x "*.git*" \
            -x "node_modules/*" \
            -x "dist/*" \
            -x "*.yml" \
            -x ".github/*"
          echo "Plugin packaged:"
          unzip -l dist/aq-${{ steps.info.outputs.id }}.zip

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.info.outputs.version }}
          name: v${{ steps.info.outputs.version }}
          generate_release_notes: true
          files: dist/aq-${{ steps.info.outputs.id }}.zip
